version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/*"
    schema: "sql/schema.sql"
    gen:
      go:
        package: "db"
        out: "sqlc/db"
        sql_package: "pgx/v5"
        emit_db_tags: true 
