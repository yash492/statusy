openapi: 3.0.0
info:
  title: Statusy
  version: 0.0.0
tags: []
paths:
  /api/services/{serviceSlug}/components:
    get:
      operationId: getServiceComponents
      description: Get all components and groups for a service
      parameters:
        - name: serviceSlug
          in: path
          required: true
          description: The service slug
          schema:
            type: string
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ServiceComponents'
  /api/statuspages:
    get:
      operationId: listStatuspages
      description: List all available status pages with optional search filter
      parameters:
        - name: search
          in: query
          required: false
          description: Search term to filter status pages by name or slug
          schema:
            type: string
          explode: false
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Statuspage'
  /api/statuspages/{statuspageSlug}:
    get:
      operationId: statuspageBySlug
      description: Get statuspage by slug
      parameters:
        - name: statuspageSlug
          in: path
          required: true
          description: Unique identifier of the status page
          schema:
            type: string
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Statuspage'
  /api/statuspages/{statuspageSlug}/feed.atom:
    get:
      operationId: getAtomFeed
      description: Get Atom feed for a status page
      parameters:
        - name: statuspageSlug
          in: path
          required: true
          description: Unique identifier of the status page
          schema:
            type: string
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/atom+xml; charset=utf-8:
              schema:
                type: string
  /api/statuspages/{statuspageSlug}/feed.rss:
    get:
      operationId: getRssFeed
      description: Get RSS feed for a status page
      parameters:
        - name: statuspageSlug
          in: path
          required: true
          description: Unique identifier of the status page
          schema:
            type: string
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/rss+xml; charset=utf-8:
              schema:
                type: string
  /api/statuspages/{statuspageSlug}/incidents:
    get:
      operationId: incidentByStatuspage
      description: Get all incidents for a specific status page
      parameters:
        - name: statuspageSlug
          in: path
          required: true
          description: Unique identifier of the status page
          schema:
            type: string
        - name: page_number
          in: query
          required: false
          description: Page number for incident updates
          schema:
            type: integer
          explode: false
        - name: page_size
          in: query
          required: false
          description: Number of updates per page
          schema:
            type: integer
          explode: false
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatuspageIncidents'
  /api/statuspages/{statuspageSlug}/schedule-maintenances:
    get:
      operationId: scheduledMaintenanceByStatuspage
      description: Get all scheduled maintenance windows for a status page
      parameters:
        - name: statuspageSlug
          in: path
          required: true
          description: Unique identifier of the status page
          schema:
            type: string
        - name: page_number
          in: query
          required: false
          description: Page number for scheduled maintenance updates
          schema:
            type: integer
          explode: false
        - name: page_size
          in: query
          required: false
          description: Number of updates per page
          schema:
            type: integer
          explode: false
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StatuspageScheduledMaintenances'
  /api/views/default:
    post:
      operationId: getDefaultView
      description: Get or create default view
      parameters: []
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/View'
  /api/views/{viewSlug}:
    put:
      operationId: editView
      description: Edit a view's details
      parameters:
        - name: viewSlug
          in: path
          required: true
          description: The view slug
          schema:
            type: string
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/View'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EditViewRequest'
    delete:
      operationId: deleteView
      description: Delete a view
      parameters:
        - name: viewSlug
          in: path
          required: true
          description: The view slug
          schema:
            type: string
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
  /api/views/{viewSlug}/services:
    post:
      operationId: addViewService
      description: Add a service to a view with component configuration
      parameters:
        - name: viewSlug
          in: path
          required: true
          description: The view slug
          schema:
            type: string
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViewServiceResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AddViewServiceRequest'
  /api/views/{viewSlug}/services/{serviceId}:
    put:
      operationId: editViewService
      description: Edit a service's component configuration in a view
      parameters:
        - name: viewSlug
          in: path
          required: true
          description: The view slug
          schema:
            type: string
        - name: serviceId
          in: path
          required: true
          description: The service ID
          schema:
            type: integer
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ViewServiceResponse'
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EditViewServiceRequest'
    delete:
      operationId: deleteViewService
      description: Remove a service from a view
      parameters:
        - name: viewSlug
          in: path
          required: true
          description: The view slug
          schema:
            type: string
        - name: serviceId
          in: path
          required: true
          description: The service ID
          schema:
            type: integer
      responses:
        '204':
          description: 'There is no content to send for this request, but the headers may be useful. '
  /api/views/{viewSlug}/unconfigured-services:
    get:
      operationId: getUnconfiguredServices
      description: Get all services not configured in a view
      parameters:
        - name: viewSlug
          in: path
          required: true
          description: The view slug
          schema:
            type: string
        - name: search
          in: query
          required: false
          description: Search term to filter services by name or slug
          schema:
            type: string
          explode: false
      responses:
        '200':
          description: The request has succeeded.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Statuspage'
components:
  schemas:
    AddViewServiceRequest:
      type: object
      required:
        - service_id
        - include_all_components
      properties:
        service_id:
          type: integer
          description: The ID of the service to add to the view
        include_all_components:
          type: boolean
          description: Whether to include all components of the service
        component_ids:
          type: array
          items:
            type: integer
          description: List of specific component IDs to include (ignored if include_all_components is true)
        component_group_ids:
          type: array
          items:
            type: integer
          description: List of specific component group IDs to include (ignored if include_all_components is true)
    Component:
      type: object
      required:
        - id
        - name
        - provider_id
      properties:
        id:
          type: integer
          description: Unique identifier for the component
        name:
          type: string
          description: Display name of the component
        provider_id:
          type: string
          description: Provider-specific identifier for the component
    ComponentGroup:
      type: object
      required:
        - id
        - name
        - provider_id
        - components
      properties:
        id:
          type: integer
          description: Unique identifier for the component group
        name:
          type: string
          description: Display name of the component group
        provider_id:
          type: string
          description: Provider-specific identifier for the component group
        components:
          type: array
          items:
            $ref: '#/components/schemas/Component'
          description: List of components within this group
    EditViewRequest:
      type: object
      required:
        - name
        - slug
        - description
        - is_default
      properties:
        name:
          type: string
          description: Display name of the view
        slug:
          type: string
          description: URL-friendly slug for the view
        description:
          type: string
          description: Description of the view
        is_default:
          type: boolean
          description: Whether this is the default view
    EditViewServiceRequest:
      type: object
      required:
        - include_all_components
      properties:
        include_all_components:
          type: boolean
          description: Whether to include all components of the service
        component_ids:
          type: array
          items:
            type: integer
          description: List of specific component IDs to include (ignored if include_all_components is true)
        component_group_ids:
          type: array
          items:
            type: integer
          description: List of specific component group IDs to include (ignored if include_all_components is true)
    ErrorResponse:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          description: Error code identifier
        message:
          type: string
          description: Human-readable error message
    Incident:
      type: object
      required:
        - id
        - title
        - status
        - incident_url
        - provider_created_at
      properties:
        id:
          type: integer
          description: Unique identifier for the incident
        title:
          type: string
          description: Title or summary of the incident
        status:
          type: string
          description: Current status of the incident (investigating, identified, monitoring, resolved)
        incident_url:
          type: string
          description: The URL of the actual status page incident
        provider_created_at:
          type: string
          format: date-time
          description: Timestamp when the incident was created by the provider
    NotFoundError:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          description: Error code identifier
        message:
          type: string
          description: Human-readable error message
    PaginationQuery:
      type: object
      properties:
        page_number:
          type: integer
          description: 'Page number for paginated results (default: 1)'
        page_size:
          type: integer
          description: 'Number of items per page (default: 20)'
    ScheduledMaintenance:
      type: object
      required:
        - id
        - title
        - status
        - scheduled_maintenance_url
        - starts_at
        - ends_at
        - provider_created_at
      properties:
        id:
          type: integer
          description: Unique identifier for the incident
        title:
          type: string
          description: Title or summary of the incident
        status:
          type: string
          description: Current status of the incident (investigating, identified, monitoring, resolved)
        scheduled_maintenance_url:
          type: string
          description: The URL of the actual scheduled maintenance event
        starts_at:
          type: string
          format: date-time
          description: The start time of the scheduled maintenance event in UTC.
        ends_at:
          type: string
          format: date-time
          description: The end time of the scheduled maintenance event in UTC
        provider_created_at:
          type: string
          format: date-time
          description: Timestamp when the incident was created by the provider
    ServiceComponents:
      type: object
      required:
        - service_id
        - service_name
        - service_slug
        - grouped_components
        - ungrouped_components
      properties:
        service_id:
          type: integer
          description: The service ID
        service_name:
          type: string
          description: The service name
        service_slug:
          type: string
          description: The service slug
        grouped_components:
          type: array
          items:
            $ref: '#/components/schemas/ComponentGroup'
          description: List of component groups for the service
        ungrouped_components:
          type: array
          items:
            $ref: '#/components/schemas/Component'
          description: List of ungrouped components for the service
    Statuspage:
      type: object
      required:
        - id
        - name
        - slug
        - url
      properties:
        id:
          type: integer
          description: Unique identifier for the status page
        name:
          type: string
          description: Display name of the status page
        slug:
          type: string
          description: URL-friendly slug for the status page
        url:
          type: string
          description: Root domain URL of the status page
    StatuspageIncidents:
      type: object
      required:
        - incidents
        - statuspage
        - total_count
      properties:
        incidents:
          type: array
          items:
            $ref: '#/components/schemas/Incident'
        statuspage:
          $ref: '#/components/schemas/Statuspage'
        total_count:
          type: integer
    StatuspageScheduledMaintenances:
      type: object
      required:
        - scheduled_maintenances
        - statuspage
        - total_count
      properties:
        scheduled_maintenances:
          type: array
          items:
            $ref: '#/components/schemas/ScheduledMaintenance'
        statuspage:
          $ref: '#/components/schemas/Statuspage'
        total_count:
          type: integer
    View:
      type: object
      required:
        - id
        - name
        - slug
        - description
        - is_default
        - services
      properties:
        id:
          type: integer
          description: Unique identifier for the view
        name:
          type: string
          description: Display name of the view
        slug:
          type: string
          description: URL-friendly slug for the view
        description:
          type: string
          description: Description of the view
        is_default:
          type: boolean
          description: Whether this is the default view
        services:
          type: array
          items:
            $ref: '#/components/schemas/ViewServiceStatus'
          description: Configuration of services in this view
    ViewServiceResponse:
      type: object
      required:
        - id
        - service_id
        - include_all_components
      properties:
        id:
          type: integer
          description: Unique identifier for the view-service mapping
        service_id:
          type: integer
          description: The ID of the service
        include_all_components:
          type: boolean
          description: Whether all components of this service are included
    ViewServiceStatus:
      type: object
      required:
        - id
        - name
        - slug
        - status
        - last_incident
        - include_all_components
      properties:
        id:
          type: integer
          description: Unique identifier for the status page service
        name:
          type: string
          description: Display name of the service
        slug:
          type: string
          description: URL-friendly slug of the service
        status:
          type: string
          description: Aggregated status of the service (up or down)
        last_incident:
          type: string
          description: Active incident description (empty string if none)
        include_all_components:
          type: boolean
          description: Whether all components of this service are included in the view
servers:
  - url: https://statusy.app
    description: A Status Page Aggregator
    variables: {}
